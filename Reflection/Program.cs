using ClassInformation;
using AccessPrivateField;
using InvokePrivateMethods;
using DynamicallyCreateObjects;
using DynamicMethodInvocation;
using RetriveAttributes;
using AccessAndModify;

namespace Reflection
{
    public class Program
    {
        public static void Main()
        {
            // ClassInfo ci = new ClassInfo();
            // ci.Display();

            // AccessPrivate ap = new AccessPrivate();
            // ap.Access();

            // InvokePrivateMethod ip = new InvokePrivateMethod();
            // ip.InvokeMethod();

            // CreateObjects co = new CreateObjects();
            // co.CreateObj();

            // MethodInvocation mi = new MethodInvocation();
            // mi.Invocation();

            // RetriveAttribute ra = new RetriveAttribute();
            // ra.AuthorInfo();

            // AccessAndModify.Student.ShowCollege();
            // ModifyStatic.ChangeCollegeName();
            // AccessAndModify.Student.ShowCollege();

        }
    }
}
